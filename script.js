// Landing Page by David S.

// Prompt user for data
let name = prompt("What is your name?");
let focus = promt("What is your main focus for today?");

// Output Data into the page
document.getElementById("nameSpan").innerHTML = name;
